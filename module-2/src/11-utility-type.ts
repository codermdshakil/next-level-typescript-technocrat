{
    /**
     * Topic: Utility Type
     * 
     * | Utility Type     | Purpose                           |
     * | ---------------  | --------------------------------- |
     * | Partial<T>       | All properties optional           |
     * | Required<T>      | All properties required           |
     * | Readonly<T>      | All properties readonly           |
     * | Pick<T, K>       | Select only K keys                |
     * | Omit<T, K>       | Remove keys K                     |
     * | Record<K, T>     | Object with keys K and values T   |
     * | Exclude<T, U>    | Remove U from T (in unions)       |
     * | Extract<T, U>    | Keep only shared types            |
     * | NonNullable<T>   | Remove `null` and `undefined`     |
     * | ReturnType<T>    | Get return type of a function     |
     * | Parameters<T>    | Get parameter types of a function |
     * 
     * 
    */



}